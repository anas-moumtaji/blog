<%  if @utilisateur.errors.any?  %>
  <div id="errors_explanation">
    <div class="alert alert-danger">
      The form contain <%= pluralize(@utilisateur.errors.count,"error") %>.
    </div>

    <ul>
      <% @utilisateur.errors.full_messages.each do |msg| %>
        <li> <%= msg %> </li>
    <% end %>
    </ul>
  </div>

<% end %>
